
from dotenv import load_dotenv
import os

load_dotenv()  # take environment variables from .env.

#загрузка токена

import os

from dotenv import load_dotenv

load_dotenv()  # take environment variables from .env.

# загрузка токена
TOKEN = os.getenv("TOKEN")
